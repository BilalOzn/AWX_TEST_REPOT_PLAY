---
- name: Backup Cisco Devices Configuration
  hosts: '{{ TARGET }}'
  gather_facts: false
  tasks:
    - name: Display lepath de stockage de backup
      ansible.builtin.debug:
        var: inventoty_hostname
        
    - name: récupérer la Running Config
      cisco.ios.ios_config:
        backup: true
        
